/* Personalized practice priorities: Mentorship Roadmap + Atlas concepts + coach signals → Zoukable recommendations. */
(() => {
'use strict';
const URL='https://lftguwmyagkehqmaxjig.supabase.co';
const KEY='sb_publishable_YzhfBB0z3emKU-rM8f_18A_Z0RUJUYt';
const $=s=>document.querySelector(s);
const esc=(v='')=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
let db=null,user=null,profile=null,roadmap=[],notes=[],assignments=[],reviews=[],skills=[],drills=[],loading=false,uiQueued=false,lastSig='',lastOrderSig='';

function style(){if($('#practice-priorities-style'))return;const s=document.createElement('style');s.id='practice-priorities-style';s.textContent=`
.practice-priorities{margin:0 0 22px;padding:18px;border:1px solid rgba(154,229,255,.22);border-radius:22px;background:radial-gradient(circle at 0 0,rgba(98,179,221,.13),transparent 34%),linear-gradient(145deg,#101b2e,#0b1424);color:#f7f7ff}.practice-priorities-head{display:flex;justify-content:space-between;gap:14px;align-items:flex-end;flex-wrap:wrap;margin-bottom:11px}.practice-priorities h2{margin:3px 0}.practice-priority-kicker{font-size:.74rem;letter-spacing:.11em;text-transform:uppercase;font-weight:850;color:#9eeaff}.practice-priority-muted{color:#aab8cf}.practice-priority-list{display:grid;gap:9px}.practice-priority-row{padding:13px 14px;border:1px solid #ffffff13;border-radius:16px;background:#ffffff06;display:grid;grid-template-columns:auto 1fr auto;gap:11px;align-items:center}.practice-priority-row:first-child{border-color:#9eeaff38;background:#15304766}.practice-priority-rank{width:34px;height:34px;border-radius:50%;display:grid;place-items:center;background:#ffffff09;font-weight:900;color:#dffaff}.practice-priority-row h3{margin:0 0 3px;font-size:1rem}.practice-priority-reasons{display:flex;gap:6px;flex-wrap:wrap;margin-top:7px}.practice-priority-reasons span{font-size:.7rem;padding:4px 7px;border-radius:999px;border:1px solid #ffffff17;background:#ffffff08;color:#c5d1e5}.practice-priority-actions{display:flex;gap:7px;align-items:center;flex-wrap:wrap;justify-content:flex-end}.practice-priority-action,.practice-priority-why{border:1px solid #9eeaff40;border-radius:999px;background:#14334b;color:#e7fbff;padding:8px 11px;font:inherit;font-size:.78rem;font-weight:850;cursor:pointer;text-decoration:none;white-space:nowrap}.practice-priority-why{background:#ffffff08;border-color:#ffffff20}.practice-priority-action[disabled]{opacity:.45;cursor:not-allowed}.practice-priority-detail{grid-column:2/4;padding:11px 12px;border-radius:13px;background:#091424;border:1px solid #ffffff12;color:#bac8dc;font-size:.81rem;line-height:1.45}.practice-priority-detail ul{margin:5px 0 0;padding-left:18px}.practice-priority-detail strong{color:#e7faff}.roadmap-priority-highlight{outline:2px solid rgba(158,234,255,.34);outline-offset:2px}.native-priority-why{margin:10px 0 0;padding:9px 10px;border-radius:12px;background:#12304714;border:1px solid #8fdbf328;font-size:.74rem;color:#53677b}.native-priority-why b{color:#264c61}.practice-priority-help{margin:12px 0 0;font-size:.78rem;color:#92a6c4}
@media(max-width:650px){.practice-priority-row{grid-template-columns:auto 1fr}.practice-priority-actions,.practice-priority-detail{grid-column:2}.practice-priority-actions{justify-content:flex-start}}
`;document.head.appendChild(s);}
function latestNote(skillId){return notes.filter(n=>n.skill_id===skillId).sort((a,b)=>new Date(b.created_at)-new Date(a.created_at))[0]||null;}
function roadmapForSkill(skill){if(!skill)return[];return roadmap.filter(item=>{
 if(!item.curriculum_node_id)return false;
 const links=window.GAB_CURRICULUM_LINKS?.forConcept(item.curriculum_node_id)||[];
 return links.some(x=>x.skill===skill.slug);
});}
function dueForDrill(id){return reviews.some(r=>r.drill_id===id&&new Date(r.due_at)<=new Date());}
function assignedForDrill(id){return assignments.some(a=>a.drill_id===id&&a.status==='active');}
function roadmapWeight(items){let n=0;for(const x of items){if(x.horizon==='next')n+=45;else if(x.horizon==='coming')n+=25;else n+=8;if(x.status==='in_progress')n+=20;if(x.pinned)n+=20;}return n;}
function conceptName(item){return item?.curriculum_node_id?(window.GAB_CURRICULUM?.displayName(item.curriculum_node_id)||item.curriculum_node_id):null;}
function scored(){return drills.filter(d=>d.status==='published').map(d=>{
 const s=skills.find(x=>x.id===d.primary_skill_id),rm=roadmapForSkill(s),note=latestNote(d.primary_skill_id),assigned=assignedForDrill(d.id),due=dueForDrill(d.id);let score=0;const reasons=[],details=[];
 if(assigned){score+=120;reasons.push('Teacher homework');details.push('Gab assigned this drill directly as active mentorship practice.');}
 if(due){score+=90;reasons.push('Review due');details.push('A previous practice context is due for spaced review. Returning after a delay is part of the learning plan.');}
 const rw=roadmapWeight(rm);if(rw){score+=rw;const top=rm.slice().sort((a,b)=>(Number(b.pinned)-Number(a.pinned))||(a.horizon==='next'?-1:1))[0],concept=conceptName(top);reasons.push(`Roadmap · ${top?.title||'current focus'}`);details.push(`${top?.title||'This topic'} is on your ${top?.horizon||'current'} Mentorship Roadmap${concept?` and maps through Atlas concept “${concept}” to ${s?.name||'this practice skill'}`:''}.`);}
 if(note){score+=(Number(note.priority)||1)*15;const label=Number(note.priority)>=3?'High teacher priority':Number(note.priority)===2?'Teacher priority':'Teacher focus';reasons.push(label);details.push(`${s?.name||'This skill'} has a current teacher-priority signal. The private observation itself is not shown here.`);}
 return {drill:d,skill:s,score,reasons,details,roadmap:rm,note,assigned,due};
 }).filter(x=>x.score>0).sort((a,b)=>b.score-a.score||a.drill.title.localeCompare(b.drill.title));}
function availableOnPractice(item){const btn=document.querySelector(`[data-drill="${CSS.escape(item.drill.id)}"]`);return btn&&!btn.disabled?btn:null;}
function signature(items,chapter){return chapter+'|'+items.slice(0,4).map(x=>`${x.drill.id}:${x.score}:${x.reasons.join(',')}`).join('|');}
function explanation(x){return `<strong>Why this is here</strong><ul>${x.details.map(d=>`<li>${esc(d)}</li>`).join('')}</ul><p style="margin:8px 0 0">This is a recommendation signal, not a mastery judgment. Existing safety clearance, prerequisites, role/side, space, energy and music filters still decide whether the drill can actually be practiced.</p>`;}
function render(){
 if(profile?.role!=='mentee')return;
 const chapter=document.body.dataset.chapter,old=$('#practice-priorities');if(!['today','practice'].includes(chapter)){old?.remove();clearAnnotations();lastSig='';lastOrderSig='';return;}
 let items=scored();if(chapter==='practice'){items=items.filter(x=>availableOnPractice(x));reorderLibrary(items);annotateLibrary(items);}
 items=items.slice(0,3);if(!items.length){old?.remove();if(chapter==='practice')clearAnnotations();lastSig='';return;}
 const intro=$('#view .intro');if(!intro)return;const sig=signature(items,chapter);if(old&&sig===lastSig)return;
 let box=old;if(!box){box=document.createElement('section');box.id='practice-priorities';box.className='practice-priorities';intro.insertAdjacentElement('afterend',box);}
 box.innerHTML=`<div class="practice-priorities-head"><div><div class="practice-priority-kicker">MENTORSHIP → ATLAS → ZOUKABLE</div><h2>Practice priorities</h2><div class="practice-priority-muted">Your roadmap and teacher signals help decide what deserves attention next.</div></div></div><div class="practice-priority-list">${items.map((x,i)=>`<div class="practice-priority-row" data-priority-row="${esc(x.drill.id)}"><span class="practice-priority-rank">${i+1}</span><div><h3>${esc(x.drill.title)}</h3><div class="practice-priority-muted">${esc(x.skill?.name||'Practice')}</div><div class="practice-priority-reasons">${x.reasons.slice(0,3).map(r=>`<span>${esc(r)}</span>`).join('')}</div></div><div class="practice-priority-actions"><button class="practice-priority-why" data-priority-why="${esc(x.drill.id)}" aria-expanded="false">Why this?</button>${chapter==='practice'?`<button class="practice-priority-action" data-priority-start="${esc(x.drill.id)}">Practice →</button>`:`<a class="practice-priority-action" href="/zoukable/?page=practice">Open practice →</a>`}</div><div class="practice-priority-detail" data-priority-detail="${esc(x.drill.id)}" hidden>${explanation(x)}</div></div>`).join('')}</div><p class="practice-priority-help">Recommendation order is transparent and reversible. The app does not infer mastery from roadmap placement or teacher priority.</p>`;
 box.querySelectorAll('[data-priority-start]').forEach(b=>b.onclick=()=>{const native=document.querySelector(`[data-drill="${CSS.escape(b.dataset.priorityStart)}"]`);native?.click();});
 box.querySelectorAll('[data-priority-why]').forEach(b=>b.onclick=()=>{const d=box.querySelector(`[data-priority-detail="${CSS.escape(b.dataset.priorityWhy)}"]`),open=d&&!d.hidden;if(!d)return;d.hidden=open;b.setAttribute('aria-expanded',String(!open));b.textContent=open?'Why this?':'Hide why';});
 lastSig=sig;
}
function reorderLibrary(items){if(!items.length)return;const rankedIds=items.map(x=>x.drill.id),buttons=[...document.querySelectorAll('[data-drill]')],parent=buttons[0]?.closest('.grid');if(!parent)return;const cards=buttons.map(b=>({id:b.dataset.drill,card:b.closest('article.card')})).filter(x=>x.card),currentPriority=cards.filter(x=>rankedIds.includes(x.id)).map(x=>x.id).join('|'),wanted=rankedIds.filter(id=>cards.some(x=>x.id===id)).join('|');if(currentPriority===wanted&&lastOrderSig===wanted)return;const ranked=new Map(rankedIds.map((id,i)=>[id,i])),priority=cards.filter(x=>ranked.has(x.id)).sort((a,b)=>ranked.get(a.id)-ranked.get(b.id));for(const {card} of cards)card.classList.remove('roadmap-priority-highlight');for(let i=priority.length-1;i>=0;i--){priority[i].card.classList.add('roadmap-priority-highlight');if(parent.firstElementChild!==priority[i].card)parent.prepend(priority[i].card);}lastOrderSig=wanted;}
function annotateLibrary(items){const byId=new Map(items.map(x=>[x.drill.id,x]));document.querySelectorAll('[data-drill]').forEach(btn=>{const card=btn.closest('article.card'),x=byId.get(btn.dataset.drill);if(!card)return;const old=card.querySelector('.native-priority-why');if(!x){old?.remove();delete card.dataset.priorityWhySig;return;}const sig=x.reasons.slice(0,2).join('|');if(old&&card.dataset.priorityWhySig===sig)return;old?.remove();const note=document.createElement('div');note.className='native-priority-why';note.innerHTML=`<b>Recommended because:</b> ${x.reasons.slice(0,2).map(esc).join(' · ')}`;const actions=card.querySelector('.actions');actions?.insertAdjacentElement('beforebegin',note);card.dataset.priorityWhySig=sig;});}
function clearAnnotations(){document.querySelectorAll('.native-priority-why').forEach(x=>x.remove());document.querySelectorAll('.roadmap-priority-highlight').forEach(x=>x.classList.remove('roadmap-priority-highlight'));}
function schedule(){if(uiQueued)return;uiQueued=true;requestAnimationFrame(()=>{uiQueued=false;render();});}
async function load(){if(loading)return;loading=true;try{if(!window.supabase)return;db=db||window.supabase.createClient(URL,KEY);const auth=await db.auth.getUser();user=auth.data?.user||null;if(!user)return;const [pR,rR,nR,aR,rvR,sR,dR]=await Promise.all([
 db.from('profiles').select('role').eq('id',user.id).single(),
 db.from('mentorship_roadmap_items').select('id,curriculum_node_id,title,horizon,status,pinned,student_visible').eq('student_id',user.id).eq('student_visible',true).in('status',['planned','in_progress']).order('pinned',{ascending:false}).order('updated_at',{ascending:false}),
 db.from('zoukable_coach_notes').select('skill_id,priority,created_at').eq('user_id',user.id).order('created_at',{ascending:false}),
 db.from('zoukable_drill_assignments').select('drill_id,status,due_date,assigned_at').eq('student_id',user.id).eq('status','active'),
 db.from('zoukable_review_states').select('drill_id,due_at').eq('user_id',user.id),
 db.from('zoukable_skills').select('id,slug,name'),
 db.from('zoukable_drills').select('id,title,status,primary_skill_id,requires_clearance')
 ]);profile=pR.data;roadmap=rR.data||[];notes=nR.data||[];assignments=aR.data||[];reviews=rvR.data||[];skills=sR.data||[];drills=dR.data||[];schedule();}catch(e){console.warn('Practice priorities',e);}finally{loading=false;}}
function start(){style();load();new MutationObserver(schedule).observe(document.body,{childList:true,subtree:true});window.addEventListener('focus',load);document.addEventListener('visibilitychange',()=>{if(document.visibilityState==='visible')load();});}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',start,{once:true});else start();
})();
